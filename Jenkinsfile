pipeline {
    agent { 
    stages {
        stage('build') {
            steps {
                sh 'go version'
                sh 'ls -a'
                echo "Hello World from Jenkins"
            }
        }
    }
}
}
